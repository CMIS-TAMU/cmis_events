# Allow build scripts for required packages
# This is needed for @sentry/cli, sharp, and other packages that require build scripts
enable-pre-post-scripts=true
shamefully-hoist=true

# Explicitly allow build scripts for these packages (pnpm security feature)
# This prevents the warning about ignored build scripts
ignore-scripts=false

